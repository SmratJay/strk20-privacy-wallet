[package]
name = "pel_perpetuals_core"
version = "1.0.0"
edition = "2024_07"

[dependencies]
starknet = ">=2.8.0"

[dev-dependencies]
snforge_std = "0.57.0"

[[target.starknet-contract]]
sierra = true
casm = true

[scripts]
test = "snforge test"
