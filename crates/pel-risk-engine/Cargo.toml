[package]
name = "pel-risk-engine"
version = "1.0.0"
edition = "2021"
description = "PEL Private Perpetuals Off-Chain High-Throughput Risk Engine, Liquidation Keeper & Stress Simulator"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"

[dev-dependencies]
